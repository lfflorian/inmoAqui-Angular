export class Imagenes {
    id: number;
    ruta: string;
    nombre: string;

    constructor(id: number, ruta: string, nombre: string){
        this.id = id,
        this.ruta = ruta,
        this.nombre = nombre
    }
}